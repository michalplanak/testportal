// General script for MózgOnFire

document.addEventListener('DOMContentLoaded', () => {
    console.log('MózgOnFire script loaded.');

    // Example: Smooth scroll for anchor links (if any are added later)
    // document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    //     anchor.addEventListener('click', function (e) {
    //         e.preventDefault();
    //         document.querySelector(this.getAttribute('href')).scrollIntoView({
    //             behavior: 'smooth'
    //         });
    //     });
    // });

    // Dynamic year in footer (if not hardcoded)
    // const yearSpan = document.querySelector('footer p');
    // if (yearSpan && yearSpan.textContent.includes('©')) {
    //     yearSpan.textContent = `© ${new Date().getFullYear()} MózgOnFire. All rights reserved.`;
    // }
});